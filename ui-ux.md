# Sakura UI/UX 方案

版本：v0.4 · 已同步确认后的需求调整 · 2026-09-20  
配套需求：`requirements.md`  
可点击原型：`preview\index.html`

## 1. 设计目标

**让用户始终知道：评审的是哪个版本、AI 看了哪些内容、准备发布什么、哪些操作尚未发生。**

视觉使用浅色中性背景、克制的樱花粉品牌色与高对比正文。避免大面积装饰占据代码空间；问题等级、连接状态和发布结果均使用文字与图形提示，不仅靠颜色。

## 2. 信息架构

| 层级 | 内容 |
| --- | --- |
| 固定导航 | 品牌、PR 评审、评审历史、连接设置、当前用户 |
| PR 工作台 | 页面标题、演示/连接状态、搜索、筛选、PR 列表 |
| PR 上下文 | 标题、作者、轮次、版本、Team Seal 范围、Jira |
| 核心工作区 | 左侧代码差异，右侧问题与报告 |
| 发布预览 | 最终英文内容、选择评审动作、版本提示、最终确认 |

“未来功能”只展示为扩展说明，不用可点击但无意义的假入口占据导航。

## 3. 桌面布局

推荐 1440px 及以上宽度：

```text
┌──────────┬─────────────────────────────────────────────────────┐
│ Sakura   │ PR 评审                 本地工作台 · 演示模式       │
│          ├─────────────────────────────────────────────────────┤
│ PR 评审  │ 待我评审 / 待确认 / 需处理      搜索 / 状态筛选     │
│ 评审历史 ├───────────────┬─────────────────────────────────────┤
│ 连接设置 │ PR 列表       │ PR 标题、分支、版本、评审操作       │
│          │               │ 范围 / Jira / 版本健康状态          │
│          │ 当前 PR       ├──────────────────┬──────────────────┤
│          │ 待开始        │ 代码差异         │ 问题 / Jira核对  │
│          │ 已过期        │                  │ / 运行记录       │
│          │ 信息缺失      │ 文件选择         │ 中文说明与证据   │
│          │               │ 行号 / 变更行    │ 英文评论编辑     │
│          │               │                  │ 选择是否发布     │
│ 当前用户 │               ├──────────────────┴──────────────────┤
│ 本机运行 │               │ 仅人工确认后发布     预览发布 →     │
└──────────┴───────────────┴─────────────────────────────────────┘
```

- PR 列表约 260px，导航约 180px，其余留给工作区。
- 代码与报告默认约 52:48；正式版建议支持拖动分隔线和最大化阅读区。
- 1280px 时收窄列表和导航；较窄视口将代码与报告上下排列。
- 代码区域横向滚动，不将长代码强行折行。
- 发布预览使用模态框；关闭后保留编辑，不触发发布。
- 原型不实现可拖动分隔条，先验证固定分屏的信息密度。

## 4. 视觉系统

| 元素 | 方向 |
| --- | --- |
| 页面背景 | 暖灰白 `#F7F8FA` |
| 卡片 | 白色，浅灰边框，轻阴影 |
| 品牌点缀 | 浅樱花粉 `#FCECF1` |
| 主操作 | 深玫瑰 `#A63860`，白字 |
| 正文 | 深蓝灰 `#253047` |
| 次要信息 | `#626C7F` |
| 成功 | 绿色文字配浅绿底 |
| 提醒 | 深棕色文字配浅黄底 |
| 阻断 | 深红文字配浅红底 |
| 字体 | 系统中文字体；代码用 Consolas / monospace |
| 圆角 | 容器 12–16px，按钮 8px |
| 留白 | 4px 基础节奏，主要间距 12/16/24px |

不采用樱花图片、外部字体或 CDN；在无外部网络时原型仍可查看。

## 5. 页面与关键交互

### 5.1 PR 列表

- 概览数字是导航辅助，不能替代具体状态。
- 卡片展示 PR ID、标题、作者、Jira、本地状态与远端评审状态。
- 当前卡片以淡粉背景、左边线和选中语义突出。
- 搜索无结果显示明确空状态及恢复提示。
- “刷新”只刷新远端数据，不自动发起评审。

### 5.2 PR 评审工作区

- 顶部展示轮次及源/目标 commit，让版本与结论绑定。
- 范围与 Jira 成功加载后以带文字的标签展示。
- 有效报告显示“待你确认”，不写“已批准”。
- 报告页签上方常驻只读 AI 署名区，展示 Sakura / GitHub Copilot / 本轮模型，以及轮次、代码版本和人工确认状态；过期报告保留原轮次来源。
- 点击发现的代码位置，左侧切换对应文件并突出相关行。
- 每条问题展示严重程度、中文原因、范围内证据、英文草稿及“发布此意见”。
- 编辑英文草稿直接反映到发布预览；未选中的发现不进入发布内容，但仍保留在本地报告。
- 英文编辑框仅编辑正文，下方提示“发布时附加固定 AI 署名”；署名不能通过正文编辑删除。
- “删除”移除人工草稿，短暂提供撤销；不会删除已发布的远端评论。
- 报告页签提供“问题”“Jira 核对”“运行记录”，避免所有信息挤进一张长卡片。

### 5.3 启动与运行

- 评审模型由用户配置，默认 Claude Opus 5（`claude-opus-5`）。启动时冻结本轮选择；所选模型不可用时阻断，不静默降级。
- 工作区分别显示“本轮模型”和“新评审模型”。用户改设置后，已有报告与运行任务不变；重新评审才应用新配置。
- “开始评审”始终由用户触发；复审建立新轮次。
- 有已有人工草稿时，重新评审先确认；正式产品归档旧轮次而不是覆盖。
- 运行时显示阶段、经过时间、取消按钮；不展示不可验证的模型内心推理。
- 尚无完整报告时禁用发布。
- 原型以短时阶段动画演示，不调用 Copilot、不生成真实分析。

### 5.4 发布预览

- 左上明确显示“演示，无真实发布”或正式环境身份。
- 重复显示 PR 及本轮版本，提醒最终版本校验。
- 按实际发布顺序展示英文总结及所选评论。
- 每条评论末尾展示独立的英文 AI 署名及人工审核发布者；不是仅在弹窗底部放一个总声明。
- 总结编辑框之外增加只读“总结最终预览”，实时展示总结、批准覆盖理由和一次完整署名。无总结和理由时不创建仅有署名的发布内容。
- 署名样式为分隔线后的次要信息；清晰可见但不压过问题正文。编辑、切换动作、重新打开弹窗都不会重复追加署名。
- 弹窗明确标为确认后的内容预览；正式版发布前不把“Human-reviewed and published by …”当成已发生事实，原型持续标记演示。
- 三种动作不预选；用户必须手动选择。
- AI 建议请求修改而用户选择 Approve 时，展示显著冲突提示并要求覆盖理由（建议规则）。
- Request changes 无问题评论且无理由时不能确认；仅评论无内容时同样不能确认。
- 最终按钮随动作变化：“确认批准”“确认请求修改”“确认仅发布评论”；原型统一增加“模拟”前缀。
- 关闭预览不改变远端状态；点击确认后才进入发布阶段。
- 结果回执区分成功、失败和部分成功；正式产品提供逐条回执和安全重试入口。

### 5.5 严格保护

| 情况 | 表达与行为 |
| --- | --- |
| 范围不明 | 明确列出缺失依据，阻止开始评审，提示修正 PR 描述 |
| Jira 无权限/信息缺失 | 展示失败原因，不显示“需求已核对” |
| 新提交或关键输入变化 | 横幅“报告已过期”，旧报告只读，禁止发布，提供重新评审 |
| 运行失败/取消 | 保留诊断，不生成“通过”；允许重试 |
| 认证失效 | 提示重新配置，禁止反复无意义重试 |
| 部分发布成功 | 逐项列出已成功项与未完成项，不显示整体成功 |

### 5.6 历史与设置

- 历史按 PR、轮次和版本呈现，区分 AI 建议与人工决定。
- 历史报告保留原始 AI 署名和实际人工发布身份，不能被新模型设置覆盖；未来导出同样携带署名。
- 正式版支持查看不可变的历史报告和发布回执；原型历史是本次页面内的简化演示事件记录。
- 设置页展示目标集成与数据边界；原型不提供真实 Token 输入框，避免用户误输。
- 正式版提供安全凭据输入、连接检测、身份确认和清理设置，不在页面回显完整密钥。
- Copilot CLI 卡片提供有标签的“评审模型”下拉框，默认 Opus 5。修改即时应用于以后启动的评审，并以通知说明生效范围，不自动启动任务。
- 原型展示 Opus 5 / Sonnet 5 两个模拟选项，并提醒仅在当前页面保存、刷新重置；正式版按真实可用模型列表提供选项并持久保存。
- 模型切换不回写历史署名，历史事件列表显示事件所属轮次的模型；模型不可用时保留选择并说明原因，不悄悄替换。

## 6. 文案与可访问性

- 界面/报告中文，API 状态和最终发布评论英文。
- 不使用“AI 已确认没问题”；使用“未发现阻断问题，仍需人工确认”。
- 错误文案包含失败原因、影响、恢复动作。
- 键盘焦点可见；表单具有标签，按钮有明确名称。
- 模态框支持 Escape、焦点限制和关闭后返回原按钮。
- 动态任务和回执通过适当的 live region 通知，不持续打断屏幕阅读器。
- 尊重减少动画偏好；状态不只以颜色表示。

## 7. 原型审阅路径

1. 默认打开有两条问题的 PR，查看分屏、切换问题/Jira/运行记录。
2. 查看报告 AI 署名，编辑英文意见、排除一条意见，打开预览核对每条评论及总结的独立署名和三种动作。
3. 选择 Request changes 并模拟发布，查看本地历史，再开始新一轮模拟评审。
4. 选择“报告已过期”的 PR，确认不能发布；重新评审后可进入新报告。
5. 选择“Jira 读取失败”的 PR，确认开始和发布均被阻止。
6. 使用“模拟新提交”使当前报告过期，检查旧草稿不可编辑。
7. 搜索不存在的编号，查看空状态；缩窄窗口检查阅读方式。
8. 在“连接设置”将模型切换为 Sonnet 5，确认旧报告仍署 Opus 5；启动另一 PR 后，新报告和评论署 Sonnet 5。在运行期间切回 Opus 5，已启动任务仍保留 Sonnet 5。

所有 PR、人员、代码、Jira 内容和回执均为虚构演示数据；不代表实际仓库情况。

## 8. 如何打开

直接用 Chrome 打开 `D:\dev\app\sakura\preview\index.html` 即可，原型不依赖构建或网络。

也可在 PowerShell 中启动仅限本机的预览服务：

```powershell
node D:\dev\app\sakura\preview\server.cjs
```

默认地址为 `http://127.0.0.1:4178`。服务仅允许读取原型文件，不暴露上层目录或原始需求中的凭据。结束预览后在该终端按 Ctrl+C。

## 9. 本轮审阅重点

- Sakura 配色是否克制，代码与报告是否有足够阅读空间。
- PR 列表常驻是否必要，还是更偏好全屏评审。
- 中文解释和英文草稿同时呈现是否过于拥挤。
- 发布前的动作选择、冲突提示和严格阻断是否符合你的工作习惯。

目前未实现：真实 API、Copilot 调用、持久化、完整历史回放、发布部分失败恢复、可拖动分隔条。这些是正式产品需求，不应以静态原型当作已验证的集成能力。
