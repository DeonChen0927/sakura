# Sakura 需求说明

版本：v0.4 · 需求已确认，按补充要求更新  
日期：2026-09-20  
目标用户：本机使用者（单人，个人工作台）  
来源：`D:\dev\app\sakura.txt` 及本次需求讨论

确认记录：用户已确认 v0.3 需求；v0.4 按后续指示将评审模型改为用户可配置，默认 Claude Opus 5。需求确认不表示技术联调已经完成，实例信息和技术验证项仍需补齐。

> 本文是去除凭据后的需求修订稿，不包含任何真实 Token。原始文件未改动，仍含明文凭据；请撤销原凭据，并删除原文件中的敏感内容。本文不代表真实系统已经实现或接入。

## 1. 产品目标与边界

Sakura 是本地运行、通过 Chrome 使用的个人工作台。第一版提供 Bitbucket PR 评审，结合 Jira 需求和验收标准，由本机 Copilot CLI 生成报告，再由用户决定是否发布评论及更新评审状态。

“本地运行”表示应用服务、配置、缓存和历史记录保存在本机，不部署 Sakura 云端服务；不表示离线运行。Bitbucket、Jira 和 Copilot 均需联网，送往 Copilot 的代码及需求内容受组织策略、账号授权和服务条款约束。若组织不允许相应处理，应阻止运行，不切换至未经批准的服务。

Sakura 项目本身无需 Git 版本管理；允许为被评审仓库维护独立 Git 缓存。不初始化或修改用户开发目录中的仓库，不自动提交、推送或合并 PR。

## 2. 已确认的产品决策

| 编号 | 决策 |
| --- | --- |
| D01 | 单人使用的本机工具，浏览器为 Chrome。 |
| D02 | 通过本机已登录的 Copilot CLI 创建评审 session，在 Sakura 展示进度和结果。 |
| D03 | 评审模型由用户配置，默认 Claude Opus 5（`claude-opus-5`）；所选模型不可用则明确报错，不静默降级或替换。 |
| D04 | 第一版接入 Jira，读取 PR 关联需求和验收标准用于评审。 |
| D05 | 正式评审意见仅限 PR 中 Team Seal 指定范围的变更；允许只读范围外的关联代码以理解上下文。 |
| D06 | 使用独立本地 Git 缓存读取准确的 PR 版本，与日常开发目录隔离。 |
| D07 | 用户可逐条编辑、删除或排除意见，并手动选择 Approve、Request changes 或仅评论。 |
| D08 | 范围识别失败、Jira 信息读取失败、PR 出现新提交等情况采用严格保护，不将不完整报告视为通过。 |
| D09 | UI 采用浅色 Sakura 风格：克制的樱花粉点缀，保持专业工具布局。 |
| D10 | 界面和本地报告使用中文；发布至 Bitbucket 的评论使用英文。 |
| D11 | 保留功能区切换能力，未来添加其他工具；第一版不实现其他业务模块。 |
| D12 | Review report 与每条发布的 review comment 必须包含 AI 署名；人工编辑不能移除 AI 来源标识。 |

已记录用户对需求整体的确认；下文保留的“建议”“待确认”标记用于标识仍需细化的实现选项或缺失信息，不将未知的实例参数或技术能力视为已验证。

## 3. 第一版范围

### 3.1 包含

- 待评审 PR 列表、手动刷新、搜索与状态筛选。
- Bitbucket 身份识别、PR 元数据、diff、评审人状态、评论和 Jira 关联信息读取。
- Team Seal 范围识别及用户可见的识别依据。
- Jira 需求及验收标准快照与覆盖核对。
- 手动启动、取消、失败后重试评审。
- 分屏查看 diff、中文报告、问题证据和英文评论草稿。
- 人工筛选与编辑意见、发布预览、显式确认发布。
- 更新当前用户的 Approve / Request changes 评审状态，或仅发布评论。
- 多轮评审、历史版本、结果过期检查、发布失败恢复。
- 本地连接设置、凭据保护、基本操作审计。

### 3.2 不包含

- 自动修改业务代码、自动提交、自动合并、自动修复 Jira。
- 未经人工确认的评论发布或批准。
- 多用户服务、团队权限平台、云端同步。
- 第一版自动定时评审、批量批准、市场化插件系统（建议）。
- 对 Team Seal 范围外的变更生成正式发现。
- 运行被评审仓库中的构建、测试或任意脚本（建议第一版只做静态评审）。

## 4. 用户主流程

1. 完成连接配置，确认 Bitbucket 用户身份、本机 Copilot 登录、指定模型以及 Jira 访问能力。
2. 打开“PR 评审”，同步待当前用户评审的 PR。
3. 选择一个 PR，查看标题、作者、分支、提交版本、Jira 和 Team Seal 范围。
4. 点击“开始评审”，执行前置检查并冻结本轮输入快照。
5. 在工作区查看可观测的运行阶段；必要时取消。
6. 查看结果、定位 diff、核对 Jira 验收标准，编辑英文评论并选择是否发布。
7. 打开发布预览，手动选择评审动作并核对最终英文内容。
8. 确认后重新验证 PR 版本与状态，发布所选内容，再执行评审状态更新。
9. 展示可核对的回执和失败详情；后续有更新时可启动新一轮评审。

AI 建议和用户最终决定分别保存。AI “未发现阻断问题”不是对正确性的保证，更不能代替人工确认。

## 5. 功能需求

### FR-01 连接与身份

- Bitbucket 仓库初始目标为 `example-org/example-repo`。
- 原始列表地址为 `https://bitbucket.org/{workspace}/{repo}/pull-requests/?state=OPEN%2BDRAFT&user_filter=REVIEWING`，这是 Bitbucket 页面而不是 Jira 列表。
- 使用 API 获取列表，不依赖抓取页面 DOM；页面查询条件不直接等同于 API 查询参数，具体映射须联调验证。
- 用 API 返回的稳定用户标识匹配评审人，不仅通过 “显示名” 显示名匹配。
- 展示 Bitbucket 和 Copilot 的实际登录身份；不能假设两个服务共享同一个身份。
- Jira 配置包含实例地址、认证方式及必要的账号标识。Cloud 或 Data Center 尚待确认。
- 凭据通过专用设置流程提交给本地后端，建议使用 Windows Credential Manager / OS 保护存储。
- 浏览器不得持久化 Token；Token 不进入 URL、日志、文档、AI 提示、源码或 session 导出。
- 连接测试区分网络异常、认证失效、权限不足和限流，不显示伪成功。
- Copilot CLI 的登录状态必须单独探测：`copilot --version` 在未认证时同样成功，不能据此判定已登录。CLI 未认证与「模型不可用」是两类问题，必须分开报告，否则用户会反复更换模型而始终失败。
- CLI 的认证凭据优先取 Sakura 加密保存的 GitHub Token（启动 CLI 时注入 `COPILOT_GITHUB_TOKEN`），其次才是启动进程继承的环境变量或 CLI 自身登录态。界面须显示当前凭据来源：仅依赖环境变量时，换一个终端启动服务就会失效。

### FR-02 PR 列表

- 展示 PR 编号、标题、作者、更新时间、草稿标记、远端评审状态、本地评审状态和关联 Jira。
- 搜索 PR 编号、标题及 Jira key，按本地状态筛选。
- 首次加载、空列表、无搜索结果、离线缓存、连接失败具有不同呈现。
- 建议默认包含 OPEN 与 Draft；Draft 允许阅读和生成报告，但禁止状态性发布，直到转为正式 PR。此规则待确认。
- 建议手动刷新；不后台自动刷新，也不自动启动 AI 评审。刷新时间以列表页脚的「最近同步」呈现，不用横幅打断——窗口每次重新聚焦都弹提示会造成噪音，已移除。
- “Request changes”是当前评审人的评审结果，不是 PR 生命周期状态；不得用 Decline/关闭 PR 代替。
- 列表范围有两类，缺一不可：①当前 Bitbucket 身份被选为评审人的未关闭 PR；②当前身份自己发起的未关闭 PR。两者由一次 BBQL 查询合并完成（`state="OPEN" AND (reviewers.uuid="<当前身份>" OR author.uuid="<当前身份>")`），并显式裁剪返回字段；全量翻页再本地筛选会拉回数十倍无关数据，刷新会明显变慢。
- 当前身份在客户端生命周期内复用，刷新列表、读详情、发布评论不重复查询 `/user`；连接测试例外，必须真发一次请求。

### FR-03 Team Seal 范围

- 范围判定先看发起人，再看范围标记。以下两类 PR 一律评审全部变更文件，不套用 Team Seal 范围：
  - 当前 Bitbucket 身份自己发起的 PR（需要整份自查）；
  - `teams.yaml` 中 Seal 团队成员发起的 PR（本团队对自己的改动负全责）。
  Codeowner Bot 的范围标记是给「外部团队改到我们代码」划界用的，拿它限制本团队自己的 PR 会把大部分改动排除在评审之外，属于漏评。报告与提示词必须区分「规则要求全评」与「没找到标记只好全评」。
- 成员名单来自 EI-monorepo 的 `teams.yaml`（本机只读，不联网），按 `bitbucketAccountId` 与 PR 作者比对——显示名会改，PR 里的 uuid 与名单记的不是同一个标识。路径可在连接设置中单独指定，留空则取本机仓库路径下的 `teams.yaml`。
- 名单读不到（未配置、文件缺失、格式异常）时不阻断：回退为按 Team Seal 范围评审，并在前置检查中明确警告名单不可用、范围可能偏小。绝不静默当作「作者不是成员」。
- 解析 `teams.yaml` 只支持该文件既有的固定写法；遇到无法识别的结构必须报错，不允许把名单静默读成空。
- 从 PR 描述中的 `A review from team Seal is required due to changes in:` 及其明确关联的范围条目提取评审范围。
- 将条目规范化并映射至实际 diff 文件；结果展示原文、规则匹配结果及命中的变更文件。
- 必须区分范围标识、目录、文件、模块名称；不允许猜测字符串前缀或默认评审整个仓库。
- 原文位置、Markdown 层级、路径通配符、重命名及删除文件的处理需要真实脱敏样例确定。
- 标识缺失、条目歧义、未知模块、没有任何可解释的变更命中：不阻断评审，改为回退为「评审本 PR 的全部代码改动」，并在启动前和报告中明确标注这不是 Team Seal 范围，提示补充或修正来源。
- 允许只读范围外的上下文文件；正式发现必须定位于范围内变更及其影响。
- 范围内问题涉及跨模块影响时，可引用范围外上下文证据，但评论仍锚定范围内相关变更。
- 若无法使用合法行号定位，可形成明确归属范围内的 PR 级评论草稿，不随意挂到其他行。

### FR-04 Jira 需求核对

- 建议从 PR 标题、描述和分支名提取 Jira key，在启动前展示关联清单及来源，允许用户补充 issue URL/key。
- 多个 Jira issue 均应可见，不任意选择其中一个。
- 读取描述、验收标准、issue 类型、状态、相关链接及更新时间；自定义验收字段映射待实例确认。
- 为每条验收标准建立核对记录：符合 / 不符合 / 静态代码无法验证，以及相关代码证据。
- 无法从静态代码验证的事项不能标为已通过；报告必须明确需要人工验证的内容。
- Jira 无关联、内容读取失败、必需字段缺失或权限不足时，阻止本轮评审并提示补齐信息。
- 已成功读取但需求语义不清时，标记评审不完整并禁止发布，要求澄清或补充依据。
- 未提供独立验收标准时不阻断评审，降级为警告：本轮只做代码评审、不做验收核对，且不能由 AI 臆造标准；是否允许人工录入并确认标准，待用户确认。
- 保存本轮需求快照和更新时间；若关联关系或关键内容变化，原报告过期，重新评审。

### FR-05 Copilot 评审

- 每轮评审创建独立 session，记录 session ID、PR ID、轮次、模型和输入指纹。
- 使用用户在连接设置中配置的模型，默认 `claude-opus-5`；按 FR-11 在任务创建时冻结模型，并验证当前账号和 CLI 实际支持，不可用则阻断。
- 输入包括 PR 元数据、源/目标 commit、范围内 diff、允许的上下文、Jira 快照和评审规则。
- 提示以正确性、需求偏差、回归、可靠性和明确风险为主；建议默认不把纯格式偏好作为阻断发现。
- AI 输出包含：中文摘要、范围覆盖、验收核对、发现列表、英文发布草稿、不确定项及建议动作。
- 每条发现包含稳定 ID、严重程度、中文说明、证据、文件路径、旧/新侧行号、英文评论和选中状态。
- 建议严重程度为“阻断 / 重要 / 建议”；具体门槛待确认。AI 建议与人工发布动作不得混为一谈。
- 结构化结果必须由后端校验。退出码异常、超时、截断、格式不符、缺少覆盖或字段均不能转成“通过”。
- 展示正在准备代码、读取需求、分析、整理报告等真实阶段及经过时间；没有可靠依据时不显示虚假百分比。
- 支持取消并停止本轮子进程；取消或失败后保留诊断记录，但不得作为完整报告发布。
- 不把模型内部推理过程作为产品功能；展示任务事件、可核实的工具活动和最终解释。
- 不给 AI Bitbucket 写凭据或发布工具；发布操作只能由应用后端在人工确认后执行。
- 不信任仓库指令、Jira 文本、PR 描述中的操作要求，必须隔离提示注入、自动加载指令和仓库可执行内容。
- 不使用全权限启动作为只读实现。CLI 工具白名单、文件访问边界及隔离策略需验证，提示词不是安全边界。

### FR-06 报告与编辑

- 桌面端以 diff 和报告并排为主，点击发现定位对应文件和行。
- 展示已评审文件、只读上下文、未完成部分，避免“未发现”被误解为“已完整覆盖”。
- 中文报告与英文发布草稿同时可见，英文内容可以直接编辑，不在最终点击时偷偷重写或重新翻译。
- 可选择、排除、删除单条评论；删除提供撤销（建议）。
- AI 原始结果不可被用户编辑覆盖；人工修订单独存储。
- 人工修改应自动保存到本地；刷新、切换 PR 或应用重启后可恢复，保存失败必须可见。
- 发布已选评论时校验非空内容、合法锚点及范围；字符数限制以目标 API 实际规则为准。
- 报告与评论按 FR-10 展示 AI 署名，正文编辑权限不包含署名元数据的修改权限。

### FR-07 发布与状态

- 最终预览显示 PR、目标源/目标版本、轮次、所选英文评论、英文总结及明确的动作选择。
- 预览必须包含每条评论及已填写报告总结的完整署名；实际发送内容与带署名的预览一致。
- 不默认选择 Approve 或 Request changes，用户主动选择；AI 只显示建议。
- 支持三个动作：Approve、Request changes、仅评论。
- Request changes 至少包含一条问题评论或非空英文理由；仅评论必须有可发布内容。
- 即使 AI 建议不通过，用户仍可选择 Approve；建议要求填写英文覆盖理由并二次确认，规则待确认。
- 点击“确认发布”前不得产生任何远端写入。
- 确认后重新读取 PR 状态、源/目标 commit、评审范围及相关需求指纹；变化则取消本次发布并标记过期。
- 先发布评论和总结，全部成功后再更新评审状态。不因部分评论成功就更新为 Approve。
- 评论写入与状态更新不是一个原子事务：需记录逐项回执、远端评论 ID 和动作结果。
- 中途失败时显示“部分成功”，不能回报整体成功；重试前核对远端结果，仅补做尚未成功的步骤。
- 对响应超时但可能已经写入的请求，先查询远端核实，不盲目重发，避免重复评论。
- 发布期间禁用重复提交；相同发布批次具有持久化去重标识。
- 用户“仅评论”时默认保留其已有评审状态，不偷偷清除先前的 Request changes 或 Approve。
- 从 Request changes 切换为 Approve 的具体 API 顺序与状态语义需验证，失败则展示真实远端状态。
- 发布前后核对版本；无法彻底消除远端并发更新的竞态时，必须在产品中披露限制并显示过期警告，不保证未经 API 支持的原子性。

### FR-08 多轮评审与历史

- Request changes 后可再次点击“开始评审”，新建轮次而不覆盖历史。
- 每轮记录输入版本、Jira 快照、范围、session、AI 原始结果、人工修订及发布回执。
- 建议每轮重新评审完整 Team Seal 范围，同时提供相对上一轮的增量和问题跟踪。
- 上轮发现可标为仍存在 / 已修复 / 无法确认；不能仅因本轮未再提及就认定已修复。
- 新提交使旧报告过期；保留只读查看，但禁止用其发布。
- 建议同一 PR 同时只允许一轮运行，全局先串行执行；任务排队、并发上限待确认。

### FR-09 工作台扩展

- 固定功能导航，第一版包含 PR 评审、评审历史、连接设置。
- 为后续功能保留模块注册入口；模块包含 ID、名称、图标、路由及页面。
- 首版只做内部模块化，不实现第三方代码插件安装（建议）。
- 公共服务包括身份连接、任务调度、通知、本地持久化和审计。

### FR-10 AI 署名与人工发布身份

- 每份 review report（本地查看、历史回放及未来导出）必须有清晰的 AI 署名；每条发布到 Bitbucket 的行级评论、PR 级评论及报告总结均分别署名，不能只在总报告中标注一次。
- 署名包括 Sakura、实际生成工具 GitHub Copilot 及该轮实际模型名称；模型信息读取不可变的轮次元数据，不根据之后的全局设置替换，不允许 AI 自述作为唯一依据。无法确认来源或模型时应明确阻断发布。
- 中文报告署名格式：`AI 生成来源：Sakura / GitHub Copilot / Claude Opus 5`，并在相邻位置显示本轮 PR、轮次与代码版本。
- 已确认发布的内容另行注明实际人工审核发布者；草稿只能显示“待人工确认”，不能提前声称已审核或已发布。
- 英文发布署名统一放在每条正文末尾，与正文以分隔线区分。格式为：

  ```text
  ---
  AI-assisted review by Sakura using GitHub Copilot (Claude Opus 5).
  Human-reviewed and published by {发布人显示名}.
  ```

- 这里的模型和发布者是当前产品目标的示例值；正式实现必须使用本轮真实元数据及确认发布时的实际账号信息，不硬编码他人身份。
- 人工可以编辑正文，但不能删除或伪造应用管理的署名；人工大幅修改后仍标为 AI-assisted，避免将所有最终文字误称为 AI 原文。
- 署名由应用根据元数据确定性拼装，不依赖模型自行追加。正文与署名分开存储，重复打开预览或重试发布不会累加署名。
- 请求内容校验与去重必须覆盖最终带署名的正文；长度限制也包含署名。署名本身不算有效评论或修改理由，不能借此绕过空内容检查。
- 英文总结包含人工覆盖理由时，应先组合完整正文，再附加一次署名，并将最终全文呈现在预览中。
- 署名只是透明披露，不改变 Bitbucket API 的发帖账号，也不代替人工批准；应用不冒充独立 AI 账号。
- 原型必须将署名标为演示，不声称静态样例实际由指定模型生成或已真实发布。

### FR-11 用户可配置评审模型

- 在“连接设置 → Copilot CLI → 评审模型”提供模型选择，首次使用默认选中 Claude Opus 5（`claude-opus-5`）。
- 模型配置是用户级默认值，影响此后新建的评审轮次；允许用户主动切换，不自动启动评审。
- 正式版在本地持久化模型 ID 与显示名称，刷新页面和应用重启后恢复；仅首次未配置时采用默认值。无效、损坏或不可用的已保存配置必须提示修正，不能静默重置。
- 正式模型选项基于本机 Copilot CLI 支持情况与当前账号权限提供；列表获取方式及可用性检测仍需技术验证，不将硬编码原型选项当作真实可用列表。
- 原型提供 Claude Opus 5 与 Claude Sonnet 5 两个演示选项，选择即时在当前页面应用；明确提示刷新会重置、未检查真实权限。
- 所选模型不可用、身份未登录或模型列表读取失败时提示具体原因并阻止启动；不得自动改用其他模型或 `auto`。
- 每次创建任务时冻结所选模型 ID/名称。配置变更不修改已创建、排队或运行中的任务，不使已有报告过期；新一轮使用最新配置。
- PR 工作区区分“本轮模型”与“新评审模型”，避免修改配置后把旧报告误认为新模型生成。
- 运行时按冻结的模型参数调用 CLI，并验证实际使用的模型；不一致时阻断完成/发布流程，明确显示问题。
- 报告、评论署名、历史及发布回执使用该轮实际模型快照；不能读取最新配置来重新署名旧内容。

## 6. 状态模型

必须分开维护四个维度，不能以一个“状态”混用：

| 维度 | 示例 |
| --- | --- |
| PR 生命周期 | OPEN / MERGED / DECLINED，加独立 Draft 标记 |
| 当前用户远端评审状态 | 未表态 / Approved / Changes requested |
| 本地评审轮次 | 待开始 / 前置检查 / 运行中 / 待确认 / 已完结（远端已表态） / 已取消 / 失败 / 已过期 |
| 发布批次 | 未发布 / 发布中 / 部分成功 / 已成功 / 失败 |

不完整、失败、取消、过期不属于“通过”。严格保护优先于人工状态选择权限：不能用人工选择绕过缺失前提。

刷新或打开 PR 时，若发现你本人已在 Bitbucket 表态（Approved 或 Changes requested），而本地最新轮次仍停在“待确认”，该轮次自动转为“已完结（远端已表态）”，并记录原因与审计。这只是把它移出待办桶，不得记作“你在 Sakura 里确认过”；报告仍可查看，也仍可发布。

远端已不再把 PR 列为“待我评审”（已合并、已关闭、被移出评审人）时，该 PR 退出工作台列表：有评审痕迹的归档保留，评审历史仍能查到它的报告；没有任何痕迹的直接删除。远端重新列出时自动恢复。

评审历史最多保留 100 轮，超出的最旧记录连同其事件、发现与发布记录一并删除；正在运行的轮次永不删除。

## 7. 本地架构与数据（建议）

- Chrome 前端连接仅监听 `127.0.0.1` 的本地服务；不对局域网或公网开放。
- 本地后端负责 Bitbucket/Jira API、凭据读取、Git 缓存、Copilot 子进程和发布。
- 即使监听回环地址也需校验 Origin/Host，防止跨站请求；变更请求使用本地会话及 CSRF 防护。
- 数据库建议 SQLite，结构化保存 PR 快照、轮次、发现、人工修订、发布批次和审计事件。
- 文件缓存保存独立仓库对象及必要文件；与程序目录、日常开发目录和凭据存储分离。
- 仓库通过只读身份拉取；不要把带 Token 的 URL 写进 Git remote 配置。
- 内容与凭据分级：不记录 Token；日志脱敏；导出前提示代码和 Jira 内容的敏感性。
- 保留期限、最大缓存占用及清理策略待确认；不擅自删除用户数据，清理先预览影响。
- 原型仅用内存状态，刷新重置；原型不实现上述持久化和连接能力。

## 8. 验收标准

| 编号 | 场景与预期 |
| --- | --- |
| AC01 | 以 API 返回的当前用户稳定标识为准，列表只展示该身份命中的待评审 PR；分页无遗漏、无重复。 |
| AC02 | 启动前展示 Team Seal 原始依据和命中文件；无法识别范围时回退为评审全部改动，并在界面上明确标注为回退而非 Team Seal 范围。 |
| AC03 | AI 可读取关联上下文，但每条正式发现的归属均在规定评审范围内（回退时即本 PR 的全部变更）。 |
| AC04 | Jira 读取失败或必需信息缺失时禁止评审，显示可操作原因，不输出“通过”；仅缺少验收标准时以警告降级为「不做验收核对」并允许继续。 |
| AC05 | 用户所选模型不可用时明确失败，不自动换模型；session ID、模型快照与轮次可以追踪。 |
| AC06 | CLI 非零退出、输出截断或格式不符时报告不完整且不能发布。 |
| AC07 | 编辑或排除意见后，发布预览与实际发送的英文内容完全一致。 |
| AC08 | 未点击最终确认前，Bitbucket 没有任何评论或状态写入。 |
| AC09 | 人工可以选择三种动作；仅评论不改变原有远端评审状态。 |
| AC10 | PR 源/目标版本变化或关键需求变化时，旧报告变为只读过期，发布被阻止。 |
| AC11 | 发布第 N 条评论失败时保留前 N-1 条回执，不更新最终评审状态；重试不重复已成功评论。 |
| AC12 | Request changes 后新建评审轮次，旧轮次和发布记录仍可查看。 |
| AC13 | UI 区分无数据、无搜索结果、运行失败、部分发布和全部成功。 |
| AC14 | 用户开发目录的分支、工作文件和 Git 状态不因评审发生改变。 |
| AC15 | Token 不出现在浏览器持久存储、日志、AI 输入、文档和代码中。 |
| AC16 | 取消后任务停止，不再产出可发布的完成报告；新一轮可重试。 |
| AC17 | 关键操作支持键盘，状态不只用颜色表达；1280px 桌面可用，窄屏可切换阅读区。 |
| AC18 | 页面刷新/应用重启后恢复人工修订；保存失败有显著提醒（正式产品，不是原型）。 |
| AC19 | 报告在本地、历史和未来导出中保留该轮 AI 来源；每条发布评论与已填写的报告总结均有独立署名。 |
| AC20 | 编辑正文、切换动作、重新打开预览和重试后，署名不丢失、不重复；所选内容预览与发布快照完全一致。 |
| AC21 | 署名与实际生成模型及审核发布者一致；草稿不提前声称已发布，静态原型明确标记为演示。 |
| AC22 | 只有署名、没有实质正文时，不能满足 Request changes 或仅评论的内容要求；不向远端写入署名空壳。 |
| AC23 | 首次使用默认 Opus 5；用户修改模型后新轮次采用新选择。正式版刷新/重启保留配置，保存失败或配置失效有明确提示。 |
| AC24 | 修改全局模型不影响已创建/运行任务和历史报告；旧报告发布仍署原模型，新轮次的报告和评论署新模型。 |

## 9. 技术确认情况与待验证项

2026-09-20 在本机读取 `copilot --version` / `copilot --help`：

- 已安装 CLI `1.0.84-5`。
- 命令帮助包含非交互 `--prompt`、`--model`、`--session-id`、`--output-format json`、`--stream` 与工具访问控制等选项。
- 仅证实命令入口存在，未启动真实评审，未验证 Claude Opus 5 账号权限、事件格式和权限组合。
- 查看了 Bitbucket Cloud PR REST 官方参考，示例将 `draft` 与 `state` 分开；实际筛选和写入语义仍需联调。
- 未使用原始文件的任何凭据，未调用真实 Jira/Bitbucket API。

| 待验证 | 必须产出的依据 |
| --- | --- |
| Copilot 登录与模型 | 模型列表获取、账号可用性、用户所选模型可执行结果及实际模型核验；不得通过自动改模型掩盖失败 |
| CLI 权限隔离 | 最小读取工具可完成评审、无法写源码/发布、不会自动信任仓库指令 |
| CLI 事件与取消 | 真实 JSONL 事件样本、错误与子进程终止行为 |
| Bitbucket 身份及 PR 筛选 | 稳定用户标识、Draft/OPEN/REVIEWING 等价查询、分页样本 |
| Team Seal 范围格式 | 至少两份脱敏 PR 描述，以及重命名/多模块边界样本 |
| Jira 实例与字段 | 实例类型、基础 URL、认证方式、验收字段 ID、跨项目访问 |
| 行级评论 | diff 新旧侧、删除/重命名文件以及无法行定位时的合法请求 |
| 状态更新 | Approve、Request changes、撤销或切换旧状态的真实 API 行为及权限 |
| 部分失败恢复 | 写入超时后的核实方法、去重机制、状态更新失败的恢复流程 |

官方参考：<https://developer.atlassian.com/cloud/bitbucket/rest/api-group-pullrequests/>

## 10. 下一轮需审阅的事项

1. 视觉与工作区布局是否适合你的日常评审。
2. Draft 的发布限制、AI 严重程度门槛、覆盖 AI 结论的理由要求。
3. Jira 无独立验收字段时是否允许人工补充，以及多个关联 issue 的选择规则。
4. 第一版只做静态分析、每轮完整评审、全局串行执行是否合适。
5. 实际 Jira 连接信息及 Team Seal 脱敏样例；不要再把 Token 放到需求文档或聊天中。
6. 历史保留、缓存上限与清理策略。

需求已获用户确认，并按补充指示更新模型配置规则；UI/UX 和原型同步展示该规则。正式开发前仍需补齐实例信息与技术验证，不将需求确认等同于真实集成已打通。
